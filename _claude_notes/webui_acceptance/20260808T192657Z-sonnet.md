# WebUI acceptance run — sonnet

**When:** 20260808T192657Z · **Endpoint:** `http://127.0.0.1:9700`
**Result:** 6 passed, 0 failed · total $0.37


Generated by `server/scripts/acceptance.py`. Checks are substring/regex probes for
the known failure modes in `webui_v1_plan.md` §8, not a grade on the prose.

## ✅ net-110-wires

*the counting trap — wires are not terminals*

**Question:** How many wires are in Net 110? List them with colour, gauge and endpoints.

| Check | Result |
|---|---|
| names all four wires | ✅ found ['W047', 'W058', 'W059'] |
| says 4 wires | ✅  |
| says 8 terminals | ✅  |

**Cost:** $0.0460 · **Duration:** 12.1s · **Denials:** 0

**Tool calls:** `Read EXTRACTION_NOTES.md`, `Grep "id":\s*"?110"?|net 110|"net":\s*"?110"?  circuit_logic.json`

<details><summary>Answer</summary>

Net 110 has **4 wires** connecting **8 terminals**.

## Wires on Net 110

| Wire | Colour | Gauge | Endpoint A | Endpoint B | Notes |
|---|---|---|---|---|---|
| W047 | BLUE | 18AWG | CR-ON:A2 | TB-110:3 | |
| W058 | BLUE | 18AWG | CR-SW:14 | TB-110:1 | |
| W059 | BLUE | 16AWG | INFEED1:1 | TB-110:2 | Part of `CABLE-INFEED1` |
| W060 | BLUE | 18AWG | CR-BP:12 | TB-110:4 | |

## Terminals on the net (for reference — 8 total)
`CR-SW:14`, `TB-110:1`, `TB-110:2`, `TB-110:3`, `TB-110:4`, `INFEED1:1`, `CR-ON:A2`, `CR-BP:12`

The drawing shows all four wires as blue, matching **EXTRACTION_NOTES.md**'s note that Q2's expected answer (blue) checks out for this net. Note W059 is 16AWG while the other three are 18AWG — the infeed cable conductor is heavier gauge than the internal panel wiring.

Electrically this implies TB-110 is the physical terminal block where CR-SW's contact, the upstream infeed interface, CR-ON's coil return, and CR-BP's N.C. contact all land in common (per `circuit_logic.json` component description at line 748).

## Sources
- `EXTRACTION_NOTES.md` — full read (Q1 verification passage, corrections list).
- `circuit_logic.json` → `nets[]` entry `id: "110"` (line 2050) and its `member_terminals`; `wires[]` entries W047, W058, W059, W060 and their `description`/`net` fields (lines 5492, 5635, 5648, 5661).

</details>

## ✅ net-110-members

*ON_NET correctness across a multi-terminal net*

**Question:** List every terminal that is a member of net 110.

| Check | Result |
|---|---|
| includes CR-SW:14 | ✅  |
| includes CR-ON:A2 | ✅  |
| includes CR-BP:12 | ✅  |
| includes INFEED1:1 | ✅  |

**Cost:** $0.0683 · **Duration:** 20.2s · **Denials:** 0

**Tool calls:** `Read EXTRACTION_NOTES.md`, `Grep "id":\s*"?110"?  circuit_logic.json`, `Grep "net_id":\s*"110"  circuit_logic.json`, `Grep "110"  circuit_logic.json`, `Grep "110"  circuit_logic.json`

<details><summary>Answer</summary>

Net **110** has 8 member terminals:

| Terminal | Component | Function |
|---|---|---|
| `CR-SW:14` | CR-SW | NO_contact |
| `TB-110:1` | TB-110 | common |
| `TB-110:2` | TB-110 | common |
| `TB-110:3` | TB-110 | common |
| `TB-110:4` | TB-110 | common |
| `INFEED1:1` | INFEED1 | input (off-page, previous machine) |
| `CR-ON:A2` | CR-ON | coil |
| `CR-BP:12` | CR-BP | NC_contact |

This is the start/stop signal shared between machines — pulling net 110 to 0V energizes the CR-ON coil. It's carried by 4 wires: `W047` (CR-ON:A2↔TB-110:3), `W058` (CR-SW:14↔TB-110:1), `W059` (INFEED1:1↔TB-110:2, via `CABLE-INFEED1`), `W060` (CR-BP:12↔TB-110:4).

## Sources
`EXTRACTION_NOTES.md` (§ "How the circuit actually works" and Q1 verification note); `circuit_logic.json` → `nets[]` entry `id: "110"` (`member_terminals`), cross-checked against `wires[]` filtered on `"net": "110"`.

</details>

## ✅ net-125-troubleshoot

*the discriminator, including the green-lamp trap*

**Question:** I'm reading 24 V on net 125. Both push buttons are green and the BYPASS breaker is closed, but the machine won't run. What should I suspect?

| Check | Result |
|---|---|
| reaches CR-BP coil not energised | ✅  |
| ranks the CR1/CR2 contacts | ✅  |
| talks about the 0 V return | ✅  |
| is not fooled by the green lamps | ✅  |

**Cost:** $0.0985 · **Duration:** 40.6s · **Denials:** 0

**Tool calls:** `Read EXTRACTION_NOTES.md`, `Grep "125"|"120"|"121"|CR-BP|CR-ON|RUN  circuit_logic.json`

<details><summary>Answer</summary>

## What the reading actually shows

**The drawing shows** net `125` sitting between `CR-BP:A2` and `BYPASS-CB:2` (`circuit_logic.json` → `wires[]` entry for `W048`, and the `nets[]` entry for `125`). `CR-BP:A1` is tied permanently to the `24E-1` bus (`W025`). For the `CR-BP` coil to energize, `A1` must be at 24 V **and** `A2` must be pulled down to 0 V — through the closed `BYPASS 5A` switch (`BYPASS-CB:2→1`, net `120`→`125`) and then through the series contacts `CR2:11-14`→`CR1:11-14` all the way to `0V` (`EXTRACTION_NOTES.md` §"How the circuit actually works").

**24 V measured on net 125 is the wrong reading for a running machine.** If the bypass path were actually complete, net `125` should be pulled down close to `0V`, not sitting at 24 V. A high‑impedance meter reading 24 V there is your source voltage (from `A1` through the de‑energized coil) leaking across an open — it tells you **there is an open somewhere between net 125 and 0V**, not where.

## Candidate path (source: A1, sink you expect: 0V)

```
CR-BP:A1 → net 24E-1 (always 24V, confirmed by design)
CR-BP:A2 → net 125 → W048 → BYPASS-CB:2 ─[BYPASS 5A switch]─ BYPASS-CB:1 → net 120
   → CR2:14 ─[CR2 11-14 contact]─ CR2:11 → net 121
   → CR1:14 ─[CR1 11-14 contact]─ CR1:11 → 0V
```

Any open in that chain leaves `A2` floating high, which is exactly what you're seeing.

## Suspects, ranked

1. **`CR1` or `CR2` N.O. contacts 11‑14 not actually closing**, even though `PB1`/`PB2` are lit green. *Reason:* a lit push button proves its lamp supply is present and that the contact block moved (per the domain rule) — it proves nothing about the CR1/CR2 coil or its 11‑14 contacts downstream. This is the single most likely explanation for 24 V surviving on 125 despite "both buttons green."
2. **`BYPASS-CB` switch contact itself** — mechanically indicating "closed" but not making internal contact (breaker mechanism failed, or the handle position doesn't reflect the internal blades). Ranked second because it's a single point of failure directly in this path and DIN-rail breakers-as-switches do fail this way.
3. **A break in wire `W048`** (CR-BP:A2 to BYPASS-CB:2) or in the conductor from `BYPASS-CB:1` to net `120` — less likely than a contact failure but still on the direct path.
4. **CR1 or CR2 coil itself not energized** (i.e., `PB1`/`PB2` lamp circuit works but the coil circuit is separately broken) — this collapses into suspect 1; same net effect.

## Probe procedure

With power on, buttons "green," bypass switch closed:

1. **Meter black lead on true chassis 0V (e.g. `PS1:-` or `TB-0V`), red lead on net `120`.**
   - Reads ~0V → net 120 is genuinely pulled down; CR1/CR2 contacts are fine, problem is downstream of 120 (suspects 2–3).
   - Reads ~24V (or floating) → net 120 is *not* pulled down; CR1 and/or CR2 contacts are not actually closed despite the green lamps — go to step 2.
2. **Isolate which relay: probe `net 121` (between CR1:14 and CR2:11) against 0V.**
   - ~0V → CR1's contact is closed, CR2's is open → suspect CR2 contact block or its actuator.
   - ~24V → CR1's contact is open → suspect CR1 contact block or its actuator, regardless of what the PB1 lamp shows.
3. **If step 1 showed net 120 at 0V (contacts good):** probe across `BYPASS-CB:1` (net 120) to `BYPASS-CB:2` (net 125) with the switch closed.
   - Significant voltage drop across a closed switch confirms the switch contact is open internally (suspect 2) — replace/inspect `BYPASS-CB`.
   - No drop, but `CR-BP:A2` still reads 24 V → check continuity of `W048` between `BYPASS-CB:2` and `CR-BP:A2` (suspect 3).

## Premise to challenge

"Both push buttons are green" is not evidence that `CR1`/`CR2` are actually latched — per the domain rule, an illuminated PB proves the lamp supply and that the contact block has moved, not that the load-side contact (`11-14`) is actually making up. Given that your 24 V reading on `125` is exactly consistent with net `120` never having been pulled down, I'd verify step 1 before touching the `BYPASS` breaker at all.

## Alternatives eliminated

- **`CR-ON` path (net 110)** is a separate route to `RUN` and doesn't touch net `125` at all — not relevant to this symptom.
- **CB1/CB2** are true over-current protection upstream of the whole 24 V bus; if either had tripped you'd have 0 V on `24E-1` and the lamps wouldn't be lit either. Since the lamps are lit, `24E-1` is healthy — ruled out.
- **Net `130`/`CR-SW`** — off-sheet, only completed by the downstream machine; unrelated to the bypass path and cannot be diagnosed from this sheet.

## Sources

- `EXTRACTION_NOTES.md` — "How the circuit actually works," inference §7 (breakers-as-switches), lamp-continuity domain rule.
- `circuit_logic.json` → `components[]` entries for `CR-BP`, `CR1`/`CR2` (via `PB1`/`PB2`), `BYPASS-CB`; `nets[]` entries for `125`, `120`, `121`, `24E-1`; `wires[]` entries `W048`, `W025`, `W030`; `edges[]` entries describing the `CR-BP` coil/contact behavior (around lines 6228–6237) and the `BYPASS-CB` function (line ~6188).

</details>

## ✅ breaker-ratings

*Q28/Q34 — REVERSE and BYPASS 5 A are switches, not protection*

**Question:** What are the ratings and purposes of every circuit breaker on this drawing?

| Check | Result |
|---|---|
| CB1 is 8 A | ✅  |
| CB2 is 20 A | ✅  |
| calls the 5 A units switches | ✅  |
| does not call them protection | ✅  |

**Cost:** $0.0584 · **Duration:** 14.7s · **Denials:** 0

**Tool calls:** `Read EXTRACTION_NOTES.md`, `Grep breaker|CB1|CB2|REVERSE|BYPASS  circuit_logic.json`

<details><summary>Answer</summary>

I'll read the extraction notes first, then pull circuit breaker details from the logic file.There are four DIN-rail devices on this drawing labeled "circuit breaker," but only two are genuine over-current protection.

| ID | Rating | Genuine protection? | Purpose |
|---|---|---|---|
| **CB1** | 8A | Yes | 115VAC line protection ahead of PS1. Terminal 1 from `DISC1:T1` (net `L1-A`), terminal 2 to `PS1:L1` (net `L1-A1`), via `W009`/`W010`. Trips → PS1 loses input → entire 24VDC system goes dead. |
| **CB2** | 20A | Yes | 24VDC bus protection, downstream of `PS1:+`. Terminal 1 from `PS1:+` (net `+24V`), terminal 2 to `TB-24E1-A:1` (net `24E-1`), via `W016`/`W017`. Trips → everything on net `24E-1` loses power: both start/stop switches, the circuit-1 light, speed controller, all relay coils, external receptacle. |
| **REVERSE 5A** (`REVERSE-CB`) | 5A | **No — used as a manual switch** | Terminal 1 from net `24E-1` (`W027`), terminal 2 to net `DIR` (`W034`). Operator-closed to enable the reverse direction signal to the drive cards. Normal state: open unless operator selects reverse. |
| **BYPASS 5A** (`BYPASS-CB`) | 5A | **No — used as a manual switch** | Terminal 1 from net `120` (`W053`), terminal 2 to net `125`/`CR-BP:A2` (`W048`). Operator-closed to enable the bypass control path; `CR-BP` energizes only if this switch is closed **and** net `120` is pulled to 0V (both PB1 and PB2 on). Normal state: open unless operator selects bypass. |

Per `EXTRACTION_NOTES.md` inference #7, REVERSE-CB and BYPASS-CB retain `class: circuit_breaker` because that's the physical part number, but their function is manual selection, not protection — never describe them as protecting anything downstream.

## Sources
- `EXTRACTION_NOTES.md` — corrections §7 (inference on breaker-as-switch), "How the circuit actually works" section
- `circuit_logic.json` → `components[]` entries for `CB1`, `CB2`, `REVERSE-CB`, `BYPASS-CB`; `terminals[]` for each; `wires[]` `W009`, `W010`, `W016`, `W017`, `W027`, `W034`, `W048`, `W053`; `insights`/analysis entries at lines ~6151–6195 describing protection vs. switch function

</details>

## ✅ revision

*Q21 — 'D' is the sheet size*

**Question:** What is the drawing number and revision of this schematic?

| Check | Result |
|---|---|
| does not report a revision of D | ✅  |
| explains D is the sheet size | ✅  |

**Cost:** $0.0418 · **Duration:** 9.5s · **Denials:** 0

**Tool calls:** `Read EXTRACTION_NOTES.md`, `Grep "drawing_number"|"revision"|"title_block"|PS20115  circuit_logic.json`

<details><summary>Answer</summary>

I'll check the extraction notes first, as required each session.**Drawing number: `PS20115MLM4-2`. Revision: none printed** — `revision` is recorded as `null`.

The drawing's title block has a `SIZE` box containing `D`, which is the sheet size, not a revision letter (an earlier extraction pass mistakenly read this as "Revision D" — see `EXTRACTION_NOTES.md` correction #2). The only revision-like information on the sheet is a stamp: *"REVISED — destroy previous drawing with the same number. DATE: 04/08/2020."*

## Sources
- `EXTRACTION_NOTES.md` — correction #2 ("Revision D is wrong — D is the sheet size...")
- `circuit_logic.json` → top-level `drawing_number` (line 3) and `revision` (line 4) fields

</details>

## ✅ cr-sw-conditions

*Q67 — a confident answer is the failure*

**Question:** What conditions are needed to energise CR-SW?

| Check | Result |
|---|---|
| declines to determine it | ✅  |
| mentions net 130 | ✅  |
| mentions the downstream machine | ✅  |

**Cost:** $0.0589 · **Duration:** 22.3s · **Denials:** 0

**Tool calls:** `Read EXTRACTION_NOTES.md`, `Grep "CR-SW"  circuit_logic.json`, `Grep "id": "130"|"net_id": "130"  circuit_logic.json`, `Read circuit_logic.json`, `Read circuit_logic.json`

<details><summary>Answer</summary>

I'll read the extraction notes first, as required every session.## Answer

CR-SW's coil needs **24VDC across A1–A2** — and the A2 side (the return) cannot be completed from anything on this sheet.

**Coil path:**
- `CR-SW:A1` → net `24E-1` (the DC bus, always present when CB2 is closed and PS1 is running).
- `CR-SW:A2` → net `130` → `TB-130:1/2` → out to **both** `DISCHARGE1:4` and `INFEED1:4` — i.e., off-sheet, to the adjacent machine's interface.

Net `130`'s description states plainly: *"It only completes through the downstream machine."* (`circuit_logic.json` → `nets[]` id `130`.) There is no switch, relay contact, or terminal on this drawing that ties `130` to `0V`. So:

- **A1 side condition:** `24E-1` energized — normal whenever PS1 and CB2 (20A) are up.
- **A2 side condition:** the downstream/subordinate machine, through its own internal wiring, must pull its mating terminal (matching `DISCHARGE1:4`/`INFEED1:4`) to that machine's 0V return, closing the loop back through `TB-130`.

**I cannot determine** what specifically inside the downstream machine does that — it's outside this sheet's scope. This matches the note in `EXTRACTION_NOTES.md`'s "How the circuit actually works" section: "Nothing on this sheet can energize [CR-SW]."

**Downstream effect once energized:** `CR-SW:11-14` (N.O. contact) closes, pulling net `110` to `0V`, which is one of the three paths that can energize `CR-ON` (the others being the infeed machine directly, or `CR-BP`'s N.C. contact via net `111`).

## Sources
- `EXTRACTION_NOTES.md` — "How the circuit actually works" section, and item flagging net 130.
- `circuit_logic.json` → `nets[]` entries for `130` and `24E-1`; `components[]` entry `CR-SW`; `terminals[]` entries `CR-SW:A1`, `CR-SW:A2`, `CR-SW:11`, `CR-SW:14`.

</details>
